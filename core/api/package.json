{
  "name": "@glowing-fishstick/api",
  "version": "0.0.3",
  "description": "Core API factory and route composition module",
  "type": "module",
  "main": "index.js",
  "exports": {
    ".": {
      "import": "./index.js"
    }
  },
  "files": [
    "index.js",
    "src/"
  ],
  "scripts": {
    "test": "npm run test:integration",
    "test:integration": "vitest run --reporter=verbose tests/integration",
    "coverage": "c8 --all --include=src/**/*.js --exclude=tests/** --reporter=text --reporter=lcov --report-dir=../../coverage/core-api vitest run --reporter=verbose tests/integration",
    "lint": "eslint ."
  },
  "dependencies": {
    "@glowing-fishstick/shared": "0.0.3",
    "express": "^5.2.1"
  }
}
