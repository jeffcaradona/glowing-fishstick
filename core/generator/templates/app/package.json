{
  "name": "{{projectName}}",
  "private": true,
  "description": "{{description}}",
  "version": "0.1.2",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon --watch src --ext js,mjs,cjs,json,eta src/server.js"
  },
  "dependencies": {
    "@glowing-fishstick/app": "{{appDependencySpec}}",
    "@glowing-fishstick/shared": "{{sharedDependencySpec}}",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.13",
    "pino-pretty": "^13.1.3"
  }
}
