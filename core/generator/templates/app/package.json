{
  "name": "{{projectName}}",
  "private": true,
  "description": "{{description}}",
  "version": "0.0.1",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon --watch src --ext js,mjs,cjs,json,eta src/server.js"
  },
  "dependencies": {
    "@glowing-fishstick/app": "^{{coreVersion}}",
    "@glowing-fishstick/shared": "^{{coreVersion}}",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.13"
  }
}
