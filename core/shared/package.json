{
  "name": "@glowing-fishstick/shared",
  "version": "0.0.1",
  "description": "Core application factory and configuration module",
  "type": "module",
  "main": "index.js",
  "exports": {
    ".": {
      "import": "./index.js"
    }
  },
  "scripts": {
    "test": "npm run test:unit",
    "test:unit": "vitest run --reporter=verbose tests/unit",
    "coverage": "c8 --all --include=src/**/*.js --exclude=tests/** --reporter=text --reporter=lcov --report-dir=../../coverage/core-shared vitest run --reporter=verbose tests/unit"
  },
  "files": [
    "index.js",
    "src/"
  ],
  "dependencies": {
    "dotenv": "^17.2.4",
    "jsonwebtoken": "^9.0.3",
    "pino": "^9.0.0"
  },
  "devDependencies": {
    "pino-pretty": "^11.0.0"
  }
}
